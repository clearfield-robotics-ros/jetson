README
There's no denying that you should read this first

1_ copy the library folder AS5045 to Arduino Library folder

2_ replace the ros.h file in Arduino/Libraries/ros_lib with this one,
	it modifies the buffer size to allow odom and imu messages through
	probably works only on a Teensy

3_ use it like you mean it

